/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'alfred.lexingtonthemes.com', 'plus.unsplash.com'],
    },
  };
  
  export default nextConfig;
  